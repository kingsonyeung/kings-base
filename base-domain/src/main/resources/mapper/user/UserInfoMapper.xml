<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kings.base.domain.user.mapper.UserInfoMapper">
    <resultMap id="BaseResultMap" type="com.kings.base.domain.user.entity.UserInfo">
        <id column="user_num" property="userNum" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="VARCHAR"/>
        <result column="user_name" property="userName" jdbcType="VARCHAR"/>
        <result column="mobile" property="mobile" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        user_num, user_id, user_name, mobile, password
    </sql>

    <sql id="condition_sql">
        <!-- 查询条件 -->
        <if test="userNum != null and userNum != ''">
            and user_num = #{userNum}
        </if>
        <if test="userName != null and userName != ''">
            and user_name = #{userName}
        </if>
    </sql>

    <select id="listByPage" resultMap="BaseResultMap" parameterType="java.util.Map">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        t_user_info
        <where>
            <include refid="condition_sql"/>
        </where>
    </select>

    <select id="findByMobile" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        t_user_info
        where mobile = #{mobile}
    </select>
</mapper>